{
    "version": "0.2.0",
    "configurations": [
        

        {
            "name": "WebApp",
            "type": "blazorwasm",
            "request": "launch",
            "cwd": "${workspaceFolder}/Client"
        }
    ]
}
